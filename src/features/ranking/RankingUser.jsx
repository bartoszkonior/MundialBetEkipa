import styled from "styled-components";

const RankingUser = styled.div`
  padding: 1.6rem 2.4rem;
  background-color: #dbac34;
  border-radius: 16px;
  color: var(--color-primary);
  font-weight: 500;
`;

export default RankingUser;
